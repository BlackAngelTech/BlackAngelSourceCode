services:
  - type: web
    name: blackangel-dgx
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    disk:
      name: blackangel-sessions
      mountPath: /opt/render/project/src/BlackAngel
      sizeGB: 1
